import { Server } from 'styletron-engine-atomic'

const styletron = new Styletron()

export default function (...components) {
  for (component of components) {
    const { style = component.data()
    for (style of Object.keys(data.styles)) {
      data.styles[]
    }
  }
}
